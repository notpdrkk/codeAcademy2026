 echo "Preço original: R$" . $preco;
    if ($preco > 500){
        $desconto = 15;
        $precoFinal = $preco - ($preco * 0.15); 
    } elseif ($preco <= 500 && $preco > 200){
        $desconto = 10;
        $precoFinal = $preco - ($preco * 0.1); 
    } elseif ($preco < 200){
        $desconto = 5;
        $precoFinal = $preco - ($preco * 0.05);
    }